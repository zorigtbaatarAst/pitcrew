[package]
name = "pitcrew-model"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "The pitcrew JSON contract, as types."

[dependencies]
serde = { workspace = true }

# Only the contract tests serialise; the types themselves need nothing but the
# derive. Keeping it here means a consumer of this crate does not pull it in.
[dev-dependencies]
serde_json = { workspace = true }
